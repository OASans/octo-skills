## Claude Code auto-compact threshold override

Env var `CLAUDE_CODE_AUTOCOMPACT_PCT_OVERRIDE` (string integer 1–100) under `env` in `settings.json` overrides the default auto-compact trigger %. No top-level `autoCompact*` key exists; only the env var works. Set in `base-settings.json:14`.
