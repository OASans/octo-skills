---
name: pull
description: >
  Run `git pull` to sync with remote. Use when the user asks to pull, sync,
  or update from remote.
---

Run `git pull --no-rebase` to sync with remote. If merge conflicts occur, resolve them and complete the merge. Report what changed (new commits, files affected).
